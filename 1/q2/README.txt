does not accept empty program
does not accept empty function body

accepts calls in main even if no functions are declared (this can be checked easily in semantics)
ex:
    "aaaaa"
    bbb("aaa","ccc")


to produse parser lexer
make compile

to compile minijava program. the resulting java program will be ./results/Main.java
make execute < program.txt

to run all default test cases
make test


to clean produced files except results
make clean
